---
import SiteLayout from '../layouts/SiteLayout.astro';
import { SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import EditorialFooter from '../components/EditorialFooter.astro';
import FloatingCTA from '../components/FloatingCTA.astro';
import fs from 'node:fs';
import path from 'node:path';

export const prerender = true;

const artists = await getCollection('artists');
const featuredArtists = artists.slice(0, 4);

// Collect portfolio images from all artists for the marquee & showcase
const publicDir = path.join(process.cwd(), 'public');
const allPortfolioImages: { src: string; artist: string }[] = [];

for (const artist of artists) {
  const portfolioPath = path.join(publicDir, artist.data.galleryDir, 'Portfolio');
  try {
    if (fs.existsSync(portfolioPath)) {
      const files = fs.readdirSync(portfolioPath)
        .filter((file) => /\.avif$/i.test(file))
        .slice(0, 5); // max 5 per artist
      for (const file of files) {
        allPortfolioImages.push({
          src: `/${artist.data.galleryDir}/Portfolio/${file}`,
          artist: artist.data.name,
        });
      }
    }
  } catch (_e) { /* skip */ }
}

// Shuffle deterministically and split into two rows for marquee
const shuffled = allPortfolioImages.sort((a, b) => a.src.localeCompare(b.src));
const marqueeRow1 = shuffled.filter((_, i) => i % 2 === 0).slice(0, 10);
const marqueeRow2 = shuffled.filter((_, i) => i % 2 === 1).slice(0, 10);

// Pick 6 varied images for the featured work mosaic
const showcaseImages = shuffled.slice(0, 6);
---

<SiteLayout title={SITE_DESCRIPTION} removeMainPadding={true} hideFooter={true}>

  <FloatingCTA href="/booking" label="Book a session" />

  <div class="relative main-content-wrapper max-w-[1800px] mx-auto border-l border-r border-border">

  <!-- Persistent page grid lines -->
  <div class="page-grid-lines absolute inset-0 z-[1] pointer-events-none hidden lg:block">
    <div class="absolute top-0 bottom-0 left-[25%] w-px bg-border/40"></div>
    <div class="absolute top-0 bottom-0 left-[50%] w-px bg-border/15"></div>
    <div class="absolute top-0 bottom-0 left-[75%] w-px bg-border/40"></div>
  </div>

  <!-- Hero Section -->
  <section class="hero-section min-h-screen relative flex flex-col overflow-hidden" data-scroll-section>

    <!-- Full-bleed B&W photo -->
    <div class="absolute inset-0 z-0 pointer-events-none overflow-hidden hero-photo-wrap">
      <div class="absolute inset-0 hero-photo-layer scale-[1.03]">
        <img src="/images/united-exterior.avif" alt="United Tattoo storefront" class="w-full h-full object-cover object-center grayscale brightness-[0.5] contrast-[1.1]" />
      </div>
      <!-- Dark overlay -->
      <div class="absolute inset-0 bg-black/40"></div>
    </div>

    <!-- Grid overlay: inset frame with internal divisions -->
    <div class="absolute z-[1] pointer-events-none" style="top:88px;left:32px;right:32px;bottom:32px;">
      <!-- Outer frame -->
      <div class="absolute inset-0 border border-white/[0.10]"></div>
      <!-- Vertical dividers (wider center column ~50%) -->
      <div class="absolute top-0 bottom-0 left-[25%] w-px bg-white/[0.10]"></div>
      <div class="absolute top-0 bottom-0 left-[75%] w-px bg-white/[0.10]"></div>
      <!-- Horizontal divider -->
      <div class="absolute left-0 right-0 top-[60%] h-px bg-white/[0.10]"></div>
      <!-- Dots: outer frame corners -->
      <div class="hero-grid-dot" style="left:0;top:0"></div>
      <div class="hero-grid-dot" style="left:100%;top:0"></div>
      <div class="hero-grid-dot" style="left:0;top:100%"></div>
      <div class="hero-grid-dot" style="left:100%;top:100%"></div>
      <!-- Dots: top edge -->
      <div class="hero-grid-dot" style="left:25%;top:0"></div>
      <div class="hero-grid-dot" style="left:75%;top:0"></div>
      <!-- Dots: bottom edge -->
      <div class="hero-grid-dot" style="left:25%;top:100%"></div>
      <div class="hero-grid-dot" style="left:75%;top:100%"></div>
      <!-- Dots: left edge -->
      <div class="hero-grid-dot" style="left:0;top:60%"></div>
      <!-- Dots: right edge -->
      <div class="hero-grid-dot" style="left:100%;top:60%"></div>
      <!-- Dots: center intersections -->
      <div class="hero-grid-dot" style="left:25%;top:60%"></div>
      <div class="hero-grid-dot" style="left:75%;top:60%"></div>
    </div>

    <!-- Film grain -->
    <div class="absolute inset-0 z-[2] pointer-events-none hero-grain"></div>

    <!-- Hero Content: centered title, bottom info -->
    <div class="relative z-10 flex-1 flex flex-col">

      <!-- Title area: positioned on the horizontal grid line, left-aligned in center column -->
      <div class="absolute inset-0 z-10 flex flex-col" style="top:88px;left:32px;right:32px;bottom:32px;">
        <div class="relative flex-1">
          <!-- Title sits just above the horizontal line at 60% -->
          <div class="absolute left-0 md:left-[25%] px-6 md:px-10" style="bottom: 40%;">
            <h1 class="hero-title text-5xl md:text-7xl lg:text-8xl tracking-tight text-white" style="font-family: 'Outfit', sans-serif; font-weight: 200;">
              <span class="hero-title-line block overflow-hidden">
                <span class="inline-block">United Tattoo</span>
              </span>
            </h1>
            <div class="hero-reveal w-12 h-px bg-primary/40 mt-5"></div>
            <p class="hero-reveal font-mono text-sm md:text-base text-white/70 mt-4 max-w-md tracking-wide">
              Custom tattoos in Fountain, Colorado
            </p>
            <div class="hero-reveal mt-6 flex items-center gap-4">
              <a href="/booking" class="inline-flex items-center gap-2 px-5 py-2.5 bg-primary text-primary-foreground font-mono text-xs uppercase tracking-[0.15em] border border-primary hover:brightness-110 transition-all">
                Book a Session
              </a>
              <a href="/artists" class="font-mono text-xs uppercase tracking-[0.15em] text-white/70 border-b border-white/30 pb-0.5 hover:text-white hover:border-white transition-all">
                View Artists
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Spacer for bottom of hero -->
      <div class="pb-8 md:pb-10"></div>
    </div>
  </section>

  <section class="studio-intro relative z-20 border-t border-b border-border" data-scroll-section>
    <!-- Grid dots -->
    <div class="grid-dot" style="left:25%;top:0"></div>
    <div class="grid-dot" style="left:50%;top:0"></div>
    <div class="grid-dot" style="left:75%;top:0"></div>
    <div class="grid-dot" style="left:25%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:50%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:75%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>

    <!-- Ghost logo -->
    <div class="studio-ghost-image" aria-hidden="true">
      <img src="/images/united-logo-full.avif" alt="" loading="lazy" />
    </div>

    <!-- Two-column editorial layout -->
    <div class="intro-layout">
      <!-- Left: statement -->
      <div class="intro-statement">
        <span class="intro-kicker intro-reveal">Custom work, built with you</span>
        <h2 class="intro-headline intro-reveal">
          Bring your idea. We will shape it into a tattoo that fits your body and still reads years from now.
        </h2>
        <div class="intro-actions intro-reveal">
          <a href="/booking" class="intro-cta-primary">
            <span>Request booking</span>
            <span class="intro-cta-arrow" aria-hidden="true">→</span>
          </a>
          <a href="/artists" class="intro-cta-secondary">View artists</a>
        </div>
      </div>

      <!-- Right: metadata sidebar -->
      <div class="intro-sidebar">
        <div class="intro-meta-stack">
          <div class="intro-meta intro-reveal">
            <span class="intro-meta-label">Location</span>
            <span class="intro-meta-value">Fountain, CO</span>
          </div>
          <div class="intro-meta intro-reveal">
            <span class="intro-meta-label">Studio</span>
            <span class="intro-meta-value">{artists.length} Artists in Residence</span>
          </div>
          <div class="intro-meta intro-reveal">
            <span class="intro-meta-label">Hours</span>
            <span class="intro-meta-value">Sun — Sat, 10a–8p</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Work Marquee: Infinite Scroll Strip -->
  <section class="marquee-section relative z-20 overflow-hidden border-t border-border py-8 md:py-12" data-scroll-section>
    <div class="grid-dot" style="left:25%;top:0"></div>
    <div class="grid-dot" style="left:50%;top:0"></div>
    <div class="grid-dot" style="left:75%;top:0"></div>

    <!-- Row 1 - scrolls left -->
    <div class="marquee-track mb-4">
      <div class="marquee-scroll">
        {[...marqueeRow1, ...marqueeRow1].map((img) => (
          <div class="marquee-item">
            <img src={img.src} alt={`Tattoo by ${img.artist}`} loading="lazy" />
            <span class="marquee-credit">{img.artist}</span>
          </div>
        ))}
      </div>
    </div>

    <!-- Row 2 - scrolls right -->
    <div class="marquee-track marquee-reverse">
      <div class="marquee-scroll">
        {[...marqueeRow2, ...marqueeRow2].map((img) => (
          <div class="marquee-item">
            <img src={img.src} alt={`Tattoo by ${img.artist}`} loading="lazy" />
            <span class="marquee-credit">{img.artist}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Section Title Banner: Artists -->
  <div class="section-banner relative z-20 border-t border-b border-border py-16 md:py-20 lg:py-28 overflow-hidden" data-scroll-section>
    <!-- Grid dots at intersections -->
    <div class="grid-dot" style="left:25%;top:0"></div>
    <div class="grid-dot" style="left:50%;top:0"></div>
    <div class="grid-dot" style="left:75%;top:0"></div>
    <div class="grid-dot" style="left:25%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:50%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:75%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <!-- Subtle noise bg -->
    <div class="absolute inset-0 noise pointer-events-none"></div>
    <h2 class="artists-title relative text-center font-sans text-6xl md:text-8xl lg:text-[10rem] font-extralight text-foreground tracking-tight leading-none">The Team</h2>
    <p class="relative text-center font-mono text-[10px] text-muted-foreground/60 mt-5 uppercase tracking-[0.3em]">{artists.length} artists in residence</p>
  </div>

  <!-- Artists Section: The Index -->
  <section id="artists" class="artists-section relative z-20" data-scroll-section>
    <div class="artists-list">
      {artists.map((artist, index) => (
        <div class="artist-row group relative border-b border-border hover:bg-secondary/50 transition-colors duration-300">
          <a href={`/artists/${artist.id}`} class="block px-6 md:px-10 lg:px-16 py-8 md:py-10 flex flex-col md:flex-row items-baseline md:items-center justify-between gap-4">
            <div class="flex items-center md:items-baseline gap-4 md:gap-8 md:w-1/2">
              <span class="font-mono text-sm text-muted-foreground/40 group-hover:text-primary/60 transition-colors tabular-nums">
                {(index + 1).toString().padStart(2, '0')}
              </span>

              <!-- Mobile Avatar -->
              <img
                src={artist.data.portrait}
                alt=""
                class="w-12 h-12 rounded-full object-cover lg:hidden border border-border"
              />

              <h3 class="text-2xl md:text-3xl lg:text-4xl font-extralight font-sans text-foreground group-hover:translate-x-2 transition-transform duration-300">
                {artist.data.name}
              </h3>
            </div>

            <div class="flex items-center justify-between w-full md:w-auto gap-6 md:gap-10">
              <div class="flex flex-col">
                <span class="font-mono text-[10px] uppercase tracking-[0.2em] mb-1 text-muted-foreground/60">Specialty</span>
                <span class="font-mono text-sm text-secondary-foreground">
                  {artist.data.specialties ? artist.data.specialties[0] : 'Custom'}
                </span>
              </div>

              <span class="w-9 h-9 rounded-full flex items-center justify-center text-primary-foreground opacity-0 group-hover:opacity-100 transition-all duration-300 bg-primary">
                ↗
              </span>
            </div>
          </a>

          <!-- Hover Image Reveal (Desktop) -->
          <div class="hidden lg:block absolute top-1/2 right-24 -translate-y-1/2 w-[180px] aspect-[3/4] opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none z-10 bg-card border border-border rotate-2 group-hover:rotate-3 transition-transform shadow-2xl">
            <img
              src={artist.data.portrait}
              alt={artist.data.name}
              class="w-full h-full object-cover grayscale"
            />
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Section Title Banner: Work -->
  <div class="section-banner relative z-20 border-t border-b border-border py-16 md:py-20 lg:py-28 overflow-hidden" data-scroll-section>
    <div class="grid-dot" style="left:25%;top:0"></div>
    <div class="grid-dot" style="left:50%;top:0"></div>
    <div class="grid-dot" style="left:75%;top:0"></div>
    <div class="grid-dot" style="left:25%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:50%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:75%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="absolute inset-0 noise pointer-events-none"></div>
    <h2 class="showcase-title relative text-center font-sans text-6xl md:text-8xl lg:text-[10rem] font-extralight text-foreground tracking-tight leading-none">Recent Work</h2>
    <div class="relative flex justify-center mt-5">
      <a href="/artists" class="inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.3em] text-muted-foreground/60 hover:text-foreground transition-colors group">
        View all artists
        <span class="w-5 h-5 rounded-full border border-border flex items-center justify-center group-hover:bg-foreground group-hover:text-background transition-all text-[9px]">→</span>
      </a>
    </div>
  </div>

  <!-- Featured Work: Visual Showcase -->
  <section class="showcase-section relative z-20" data-scroll-section>
    <!-- Bordered image cells grid -->
    <div class="showcase-grid grid grid-cols-2 md:grid-cols-3">
      {showcaseImages.map((img, i) => (
        <div class="showcase-item group relative border-b border-r border-border p-2 md:p-3">
          <div class="relative overflow-hidden aspect-[4/5]">
            <img
              src={img.src}
              alt={`Tattoo by ${img.artist}`}
              loading="lazy"
              class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <span class="absolute bottom-3 left-3 font-mono text-[10px] uppercase tracking-widest text-white/90 opacity-0 group-hover:opacity-100 translate-y-2 group-hover:translate-y-0 transition-all duration-300">{img.artist}</span>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Section Title Banner: Process -->
  <div class="section-banner relative z-20 border-t border-b border-border py-16 md:py-20 lg:py-28 overflow-hidden" data-scroll-section>
    <div class="grid-dot" style="left:25%;top:0"></div>
    <div class="grid-dot" style="left:50%;top:0"></div>
    <div class="grid-dot" style="left:75%;top:0"></div>
    <div class="grid-dot" style="left:25%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:50%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="grid-dot" style="left:75%;bottom:0;top:auto;transform:translate(-50%,50%)"></div>
    <div class="absolute inset-0 noise pointer-events-none"></div>
    <h2 class="process-title relative text-center font-sans text-6xl md:text-8xl lg:text-[10rem] font-extralight text-foreground tracking-tight leading-none">The Process</h2>
    <p class="relative text-center font-mono text-[10px] text-muted-foreground/60 mt-5 uppercase tracking-[0.3em]">From idea to ink</p>
  </div>

  <!-- Process Section: Bordered Grid -->
  <section id="process" class="process-section relative z-20" data-scroll-section>
    <div class="process-grid grid grid-cols-1 md:grid-cols-2">
      {[
        { num: '01', title: 'Consultation', desc: 'Share your vision. We shape your idea together and match you with the right artist.' },
        { num: '02', title: 'Design', desc: 'Your artist creates a custom piece tailored to your body and style.' },
        { num: '03', title: 'Session', desc: 'Get inked in our clean, comfortable studio with experienced professionals.' },
        { num: '04', title: 'Aftercare', desc: 'We guide you through healing so your artwork stays vibrant for life.' }
      ].map((step, i) => (
        <div class="process-step group relative border-b border-r border-border p-8 md:p-12 lg:p-16 flex flex-col justify-between min-h-[280px] md:min-h-[360px] hover:bg-secondary/20 transition-colors duration-500">
          <!-- Corner dot -->
          <div class="absolute top-0 right-0 w-1.5 h-1.5 rounded-full bg-primary/25 translate-x-1/2 -translate-y-1/2"></div>

          <div class="flex items-start justify-between">
            <span class="process-num font-mono text-[11px] text-primary/40 tabular-nums tracking-wider">
              {step.num}
            </span>
          </div>

          <div>
            <h3 class="font-sans text-3xl md:text-4xl lg:text-5xl font-extralight text-foreground tracking-tight mb-5">
              {step.title}
            </h3>
            <p class="font-mono text-sm text-muted-foreground leading-relaxed max-w-sm">
              {step.desc}
            </p>
          </div>
        </div>
      ))}
    </div>
  </section>


  <EditorialFooter />

  </div>

  <!-- Micro-interactions & Hover Styles -->
  <style>
    /* === Marquee === */
    .marquee-track {
      overflow: hidden;
      mask-image: linear-gradient(to right, transparent, black 5%, black 95%, transparent);
      -webkit-mask-image: linear-gradient(to right, transparent, black 5%, black 95%, transparent);
    }

    .marquee-scroll {
      display: flex;
      gap: 0.75rem;
      width: max-content;
      animation: marquee-left 40s linear infinite;
    }

    .marquee-reverse .marquee-scroll {
      animation: marquee-right 45s linear infinite;
    }

    .marquee-item {
      position: relative;
      width: 240px;
      height: 180px;
      flex-shrink: 0;
      overflow: hidden;
      border-radius: 2px;
    }

    @media (min-width: 768px) {
      .marquee-item {
        width: 320px;
        height: 220px;
      }
    }

    .marquee-item img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.5s ease;
    }

    .marquee-item:hover img {
      transform: scale(1.08);
    }

    .marquee-credit {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 6px 10px;
      font-family: 'Geist Mono', monospace;
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      color: white;
      background: linear-gradient(to top, rgba(0,0,0,0.7), transparent);
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .marquee-item:hover .marquee-credit {
      opacity: 1;
    }

    @keyframes marquee-left {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    @keyframes marquee-right {
      0% { transform: translateX(-50%); }
      100% { transform: translateX(0); }
    }

    /* Pause marquee on hover */
    .marquee-track:hover .marquee-scroll {
      animation-play-state: paused;
    }

    /* === Showcase Grid === */
    .showcase-item:nth-child(3n) {
      border-right: none;
    }

    @media (max-width: 767px) {
      .showcase-item:nth-child(3n) {
        border-right: 1px solid var(--border);
      }
      .showcase-item:nth-child(2n) {
        border-right: none;
      }
    }

    /* === Section Banners === */
    .section-banner {
      position: relative;
      background: oklch(from var(--background) l c h / 0.95);
    }

    /* Artist name underline on hover */
    .artist-row:hover h3 {
      background-image: linear-gradient(to right, var(--primary), var(--ring));
      background-size: 0 2px;
      background-position: 0 100%;
      background-repeat: no-repeat;
      animation: underlineSlide 0.3s ease-out forwards;
    }

    @keyframes underlineSlide {
      to {
        background-size: 100% 2px;
      }
    }

    /* Grid intersection dots */
    .hero-grid-dot {
      position: absolute;
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--ring);
      opacity: 0.5;
      transform: translate(-50%, -50%);
    }

    /* Film grain */
    .hero-grain {
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      opacity: 0.035;
      mix-blend-mode: overlay;
    }

    /* Info column separator */
    .hero-info-col {
      position: relative;
    }

    .hero-info-col + .hero-info-col::before {
      content: '';
      position: absolute;
      left: -1rem;
      top: 50%;
      transform: translateY(-50%);
      width: 1px;
      height: 24px;
      background: oklch(from var(--foreground) l c h / 0.1);
    }

    @media (min-width: 768px) {
      .hero-info-col + .hero-info-col::before {
        left: -1.5rem;
      }
    }

    /* Mobile CTA hover effect */
    .hero-section a[href="/booking"]:hover {
      filter: brightness(1.15);
      transform: translateY(-2px);
      box-shadow: 0 10px 40px oklch(from var(--primary) l c h / 0.3);
    }

    /* Process grid: remove right border on right column */
    .process-step:nth-child(2n) {
      border-right: none;
    }

    @media (max-width: 767px) {
      .process-step {
        border-right: none !important;
      }
    }

    /* Process step hover */
    .process-step:hover .process-num {
      color: var(--ring) !important;
    }

    /* Column container */
    .main-content-wrapper {
      position: relative;
    }

    /* === Studio Intro === */
    .studio-intro {
      position: relative;
      background: var(--background);
    }

    .studio-ghost-image {
      position: absolute;
      right: clamp(1rem, 3vw, 3rem);
      top: 50%;
      transform: translateY(-50%);
      width: min(38vw, 520px);
      pointer-events: none;
      z-index: 1;
      opacity: 0.05;
      mask-image: radial-gradient(circle at center, black 34%, transparent 78%);
      -webkit-mask-image: radial-gradient(circle at center, black 34%, transparent 78%);
    }

    .studio-ghost-image img {
      width: 100%;
      height: auto;
      object-fit: contain;
      filter: grayscale(1) contrast(1.05);
    }

    @media (max-width: 1023px) {
      .studio-ghost-image {
        width: min(62vw, 420px);
        right: 0.5rem;
        opacity: 0.03;
      }
    }

    /* Layout */
    .intro-layout {
      display: grid;
      grid-template-columns: 1fr;
      position: relative;
      z-index: 2;
    }

    @media (min-width: 1024px) {
      .intro-layout {
        grid-template-columns: 1fr 280px;
      }
    }

    @media (min-width: 1280px) {
      .intro-layout {
        grid-template-columns: 1fr 320px;
      }
    }

    /* Left: statement */
    .intro-statement {
      padding: 2.5rem 1.5rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    @media (min-width: 768px) {
      .intro-statement {
        padding: 3.5rem 2.5rem;
      }
    }

    @media (min-width: 1024px) {
      .intro-statement {
        padding: 4.5rem 4rem 4.5rem clamp(2.5rem, 5vw, 5rem);
      }
    }

    .intro-kicker {
      display: block;
      font-family: var(--font-mono);
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.28em;
      color: oklch(from var(--ring) l c h / 0.75);
      margin-bottom: 1.5rem;
    }

    .intro-headline {
      font-family: var(--font-sans);
      font-weight: 200;
      font-size: clamp(1.6rem, 3.2vw, 2.6rem);
      line-height: 1.35;
      letter-spacing: -0.01em;
      color: var(--foreground);
      max-width: 600px;
      margin-bottom: 2.5rem;
    }

    .intro-actions {
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }

    .intro-cta-primary {
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.7rem 1.4rem;
      background: oklch(from var(--primary) l c h / 0.95);
      color: var(--primary-foreground);
      font-family: var(--font-mono);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      border: 1px solid var(--primary);
      transition: all 0.3s ease;
    }

    .intro-cta-primary:hover {
      filter: brightness(1.1);
      transform: translateY(-1px);
      box-shadow: 0 4px 20px oklch(from var(--primary) l c h / 0.2);
    }

    .intro-cta-arrow {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 18px;
      height: 18px;
      border-radius: 50%;
      background: oklch(from var(--primary-foreground) l c h / 0.15);
      font-size: 10px;
      transition: transform 0.3s ease;
    }

    .intro-cta-primary:hover .intro-cta-arrow {
      transform: translateX(2px);
    }

    .intro-cta-secondary {
      font-family: var(--font-mono);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      color: var(--muted-foreground);
      padding: 0.7rem 0;
      border-bottom: 1px solid oklch(from var(--border) l c h / 0.7);
      transition: all 0.3s ease;
    }

    .intro-cta-secondary:hover {
      color: var(--foreground);
      border-color: var(--foreground);
    }

    /* Right: sidebar */
    .intro-sidebar {
      display: flex;
      flex-direction: row;
      gap: 1.5rem;
      padding: 0 1.5rem 2rem;
      align-items: flex-start;
    }

    @media (min-width: 1024px) {
      .intro-sidebar {
        flex-direction: column;
        gap: 0;
        padding: 0;
        border-left: 1px solid var(--border);
        justify-content: space-between;
      }
    }

    .intro-meta-stack {
      display: flex;
      gap: 1.5rem;
      flex: 1;
    }

    @media (min-width: 1024px) {
      .intro-meta-stack {
        flex-direction: column;
        gap: 0;
      }
    }

    .intro-meta {
      flex: 1;
    }

    @media (min-width: 1024px) {
      .intro-meta {
        padding: 1.25rem 1.5rem;
        border-bottom: 1px solid oklch(from var(--border) l c h / 0.5);
      }
    }

    .intro-meta-label {
      display: block;
      font-family: var(--font-mono);
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.22em;
      color: oklch(from var(--muted-foreground) l c h / 0.65);
      margin-bottom: 0.35rem;
    }

    .intro-meta-value {
      display: block;
      font-family: var(--font-sans);
      font-size: 13px;
      font-weight: 300;
      color: var(--foreground);
      letter-spacing: 0.01em;
    }

  </style>

  <!-- Scroll Animations -->
  <script>
    // Wait for GSAP to be available
    const initAnimations = () => {
      const gsap = window.gsap;
      const ScrollTrigger = window.ScrollTrigger;

      if (!gsap || !ScrollTrigger) {
        // Retry if global GSAP isn't ready yet (rare with astro:page-load but possible)
        setTimeout(initAnimations, 50);
        return;
      }

      // Set initial states for hero elements
      gsap.set('.hero-title-line > span', { y: '110%' });
      gsap.set('.hero-reveal', { opacity: 0, y: 30 });
      gsap.set('.intro-reveal', { opacity: 0, y: 22 });

      // Subtle parallax on hero exterior photo
      const heroSection = document.querySelector('.hero-section');
      const photoLayer = document.querySelector('.hero-photo-layer');

      if (heroSection && photoLayer) {
        heroSection.addEventListener('mousemove', (e) => {
          const xPos = (e.clientX / window.innerWidth - 0.5) * 12;
          const yPos = (e.clientY / window.innerHeight - 0.5) * 8;

          gsap.to(photoLayer, {
            x: xPos,
            y: yPos,
            duration: 1.8,
            ease: 'power2.out'
          });
        });
      }

      // Hero photo reveal - cinematic fade up
      gsap.set('.hero-photo-wrap', { scale: 1.15, opacity: 0 });
      gsap.to('.hero-photo-wrap', {
        scale: 1,
        opacity: 1,
        duration: 2.4,
        ease: 'power3.out',
        delay: 0.1
      });

      // Hero Title Animation on Load
      const heroTl = gsap.timeline({ delay: 0.4 });

      heroTl
        // Title lines sweep up
        .to('.hero-title-line > span', {
          y: '0%',
          duration: 1.4,
          stagger: 0.2,
          ease: 'power4.out'
        })
        // Reveal elements stagger in
        .to('.hero-reveal', {
          opacity: 1,
          y: 0,
          duration: 1,
          stagger: 0.12,
          ease: 'power3.out'
        }, '-=0.8');

      gsap.to('.intro-reveal', {
        opacity: 1,
        y: 0,
        stagger: 0.08,
        duration: 0.8,
        ease: 'power3.out',
        scrollTrigger: {
          trigger: '.studio-intro',
          start: 'top 82%'
        }
      });

      // Hero Parallax on Scroll
      gsap.to('.hero-title', {
        yPercent: 20,
        ease: 'none',
        scrollTrigger: {
          trigger: '.hero-section',
          start: 'top top',
          end: 'bottom top',
          scrub: 1
        }
      });

      // Section Banner Animations
      gsap.utils.toArray('.section-banner h2').forEach((title) => {
        gsap.set(title, { opacity: 0, y: 40 });
        gsap.to(title, {
          opacity: 1,
          y: 0,
          duration: 1,
          ease: 'power3.out',
          scrollTrigger: {
            trigger: title.closest('.section-banner'),
            start: 'top 85%'
          }
        });
      });

      // Artists Section Animations
      gsap.set('.artist-row', { opacity: 0, y: 40 });

      gsap.to('.artist-row', {
        opacity: 1,
        y: 0,
        stagger: 0.08,
        duration: 0.7,
        ease: 'power3.out',
        scrollTrigger: {
          trigger: '.artists-list',
          start: 'top 80%'
        }
      });

      // Showcase Section Animations
      gsap.set('.showcase-item', { opacity: 0, y: 30, scale: 0.97 });

      gsap.to('.showcase-item', {
        opacity: 1,
        y: 0,
        scale: 1,
        stagger: 0.08,
        duration: 0.7,
        ease: 'power3.out',
        scrollTrigger: {
          trigger: '.showcase-grid',
          start: 'top 85%'
        }
      });

      // Process Section Animations
      gsap.set('.process-step', { opacity: 0, y: 30 });

      gsap.to('.process-step', {
        opacity: 1,
        y: 0,
        stagger: 0.1,
        duration: 0.8,
        ease: 'power3.out',
        scrollTrigger: {
          trigger: '.process-grid',
          start: 'top 85%'
        }
      });

      // Footer Animations
      gsap.set('.footer-title', { opacity: 0, scale: 0.8 });
      gsap.set('.footer-reveal', { opacity: 0, y: 40 });
      gsap.set('.footer-social', { opacity: 0, scale: 0.5 });

      const footerTl = gsap.timeline({
        scrollTrigger: {
          trigger: '.footer-section',
          start: 'top 80%'
        }
      });

      footerTl
        .to('.footer-title', {
          opacity: 1,
          scale: 1,
          duration: 1,
          ease: 'power4.out'
        })
        .to('.footer-reveal', {
          opacity: 1,
          y: 0,
          stagger: 0.1,
          duration: 0.8,
          ease: 'power3.out'
        }, '-=0.6')
        .to('.footer-social', {
          opacity: 1,
          scale: 1,
          stagger: 0.1,
          duration: 0.5,
          ease: 'back.out(2)'
        }, '-=0.4');

      console.log('Scroll animations initialized');
    };

    // Initialize when DOM is ready
    document.addEventListener('astro:page-load', initAnimations);

    // FloatingCTA visibility is handled by the FloatingCTA component itself
  </script>
</SiteLayout>
